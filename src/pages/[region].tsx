export function region() {
  //Take prop to fetch specified info from api
  //Map videos vertically & with a scroll
  //Keep a description of text fixed absolutely at the bottom 1/4th of the screen.

  const regionDiagnostics = null;
}
